# Development

Use `make install`, `make format`, `make lint`, and `make test` as the baseline
developer loop. For parity work, bootstrap and validate the ledger with the scripts in
`./scripts/` against `vendor/rig/rig/rig-core`.
