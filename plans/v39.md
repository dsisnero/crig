# v0.39.0 Porting Plan

## Upstream Baseline
- **Repository**: `https://github.com/0xPlaygrounds/rig.git`
- **Crate**: `vendor/rig/crates/rig-core`
- **Pinned commit**: `06bc651f4c64d1673ba6af698f6c66602c5d313f`
- **Crystal branch**: `port/rig-v0.39.0` (51 commits ahead of main)
- **Last upstream sync**: `eda2e17` (HEAD of vendor/rig submodule)

## Completed Chunks

### C1: Delete upstream-removed modules
- [x] Remove pipeline (6 files)
- [x] Remove buffered provider internal
- [x] Remove old hooks module
- **Gates**: source compiles, spec file valid
- **Commit**: `a3e2dcc`

### C2: Port new data-type modules
- [x] `id.rs` → `id.cr` (ID generation)
- [x] `rerank.rs` → `rerank.cr` (RerankModel trait + errors)
- [x] `provider_response.rs` → `provider_response.cr` (ProviderResponseError)
- **Gates**: source compiles
- **Commit**: `a3e2dcc`

### C3: Port structured tool results
- [x] `tool/result.rs` → `tool/result.cr`
- [x] ToolFailure, ToolFailureKind, ToolOutcome, ToolReturnOutcome
- [x] ToolExecutionResult, ToolReturn(T)
- **Tests**: 13 spec tests (RED→GREEN)
- **Commit**: `7ca65ad`

### C4: Port hook system v2 data types
- [x] RunId, Scratchpad (with Mutex), HookContext
- [x] RequestPatch (with merge logic), Flow, StepEventKind
- [x] AgentHook module, StepEvent struct
- **Tests**: 18 spec tests
- **Commits**: `41b0a6b`, `5c4c4f3`, `793f0a7`

### C5: Port AgentRun state machine
- [x] `agent/run/mod.rs` → `agent/run.cr`
- [x] AgentRun, AgentRunStep, ModelTurn, ModelTurnOutcome
- [x] next_step, model_response, tool_results, resolve_invalid_tool_call
- [x] OutputMode enum + resolution helpers
- **Tests**: 12 spec tests (text run, tool roundtrip, max_turns, repair, etc.)
- **Commits**: `b854a66`, `43b277c`, `328e0ae`

### C6: Port AgentRunner sequential
- [x] `agent/runner.rs` → `agent/runner.cr`
- [x] AgentRunner class with builder, run(), hook dispatch
- [x] Mergeable patches (CompletionCall), chained rewrites (ToolCall/ToolResult)
- **Tests**: 3 spec tests (text-only, tool roundtrip, hook recording)
- **Commit**: `793f0a7`

### C7: Concurrent tool execution
- [x] Errgroup pattern: Channel(Bool) semaphore + WaitGroup + Atomic(Bool)
- [x] execute_tools_concurrent with bounded parallelism
- [x] First-error-wins, drain in-flight
- **Tests**: 3 spec tests
- **Commit**: `1b576b7`

### C8: Streaming drive_agent
- [x] Channel generator: spawn fiber yields StreamItem to channel
- [x] stream() method on AgentRunner
- [x] StreamTextDelta, StreamToolResult, StreamDone, StreamError types
- **Tests**: 3 spec tests
- **Commit**: `c267f3e`

### C9: Scratchpad Mutex safety
- [x] Wrap all operations in Mutex#synchronize
- [x] Reentrant mutex for nested hook calls
- [x] HookContext accepts optional shared scratchpad
- **Tests**: 3 concurrency spec tests
- **Commit**: `5c4c4f3`

### C10: Wire Agent → AgentRunner bridge
- [x] Agent#runner(prompt) creates AgentRunner seeded from agent config
- [x] Agent#add_hook stores new-style AgentHooks
- [x] ToolServerHandle bypass inbox, call server directly
- [x] Agent#call uses runner().run() (matches upstream prompt().await → runner.run())
- **Tests**: 3 integration spec tests
- **Commits**: `673daf5`, `42a4557`, `1a37e56`

### C11: Fix agent-as-tool crash
- [x] Remove ToolType wrapper — store ToolDyn directly in ToolSet
- [x] Fix ToolServer spawn crash (remove nested spawn)
- [x] Fix flat_map_ordered forall compilation bug
- [x] Fix json_schema_for nilable types + enum support
- **Tests**: 9 toolserver + json_schema spec tests
- **Commits**: `d0c0e3c`, `5aa083e`, `24742ea`, `01ed56a`

### C12: Fix assistant message + tool result format
- [x] assistant_msg uses all choice items (not just .first)
- [x] Runner uses tool_result_with_call_id for provider compatibility
- [x] Fix is_empty_choice? for tool-call turns
- **Tests**: 3 spec tests (message chain, assistant content)
- **Commits**: `669e3c0`, `987f9ad`

### C13: Port v0.39.0 vendor examples
- [x] agent_run_stepping (hand-driven + hook)
- [x] tool_result_outcomes (policy on structured outcomes)
- [x] request_hook (4 stacked hooks, patch merging)
- [x] agent_with_human_in_the_loop (interactive HITL)
- [x] agent_with_approval_policy (non-interactive policy)
- [x] agent_evaluator_optimizer (generator + evaluator loop)
- [x] agent_with_agent_tool (agent-as-tool)
- [x] agent_with_durable_approval (JSON serialization + resume)
- **Gates**: each example compiles and runs with DEEPSEEK_API_KEY
- **Commits**: `328e0ae`, `0be4ee2`, `2f7c419`, `9f99a20`, `fe3c70e`, `d1be680`

### C14: Convenience API improvements
- [x] AgentRun.new(String) auto-wraps via Message.user
- [x] UserContent.tool_result(id, text) plain-string overload
- [x] ToolResultContent.from_tool_output(text) matches Rust API
- [x] ModelTurn named-argument constructor
- **Tests**: 3 spec tests
- **Commits**: `d9e0077`, `5a3bd92`, `745387c`

---

## Completed Chunks (v39 porting)

### R1: Migrate PromptRequest.send() to AgentRunner  ✓
**Size**: Medium (~200 lines changed)
- PromptRequest#send delegates to @runner.run(@prompt) instead of inline state machine
- PromptHookAdapter bridges old PromptHook → new AgentHook for backward compat
- AgentToolAdapter loses generic M parameter, uses proc-based callable
- Spec helpers moved to spec/support/test_models.cr
- Runner passes full error_history through hook dispatch
- **Gates**: run_spec (10) + prompt_request_spec (elided) pass
- **Commit**: `8108763`

### R2: Sync DeepSeek provider  ✓
**Size**: Small (~50 lines)
- Added thinking_is_disabled? and deepseek_tool_choice helpers
- Required/Specific tool_choice suppressed when thinking mode active
- reasoning_tokens propagation from completion_tokens_details
- DEEPSEEK_V4_FLASH and DEEPSEEK_V4_PRO constants verified
- **Tests**: 6 spec tests (tool_choice suppression, reasoning tokens, constants)
- **Gates**: deepseek_spec passes
- **Commit**: `d5cb627`

### R6: Sync http_client module  ✓
**Size**: Small (~30 lines)
- Removed unused Constant/Never retry policies (matches upstream removal)
- Removed unused with_retry_policy factory
- Updated RetryPolicyCarrier alias to ExponentialBackoff only
- Added Error#non_success_status and Error#non_success_body helpers
- allow_missing_content_type was already present
- **Commit**: `579397f`

### R7: Add ProviderResponseHelpers to all capability errors  ✓
**Size**: Small (~50 lines)
- Added ProviderResponse kind variant + provider_response field to all 6 missing error types
- Added from_http_response / from_provider_body factory methods
- Moved provider_response.cr require earlier in load order to break circular dep
- **Tests**: 11 spec tests across all 7 error types
- **Commit**: `a7f1a0e`

### R5: Sync Ollama provider (partial)  ✓
**Size**: Small
- think parameter changed from Bool default=false to (Bool|String)? (nil = model default)
- Added 'max' level to Think::Level enum
- Non-streaming response preserves thinking as AssistantContent::Reasoning (issue #1926)
- to_core_message round-trips thinking back as reasoning content
- **Tests**: 4 spec tests (think serialization, reasoning preservation)
- **Commit**: `73ca9aa`

---

## Remaining Chunks

### R3: Sync OpenAI Responses API to upstream
**Size**: Large (~300 lines)
**Depends on**: None
**What**: Upstream changed 1686 lines in responses_api. Port system instructions placement (SystemInstructionsPlacement enum on OpenAIResponsesExt), completion_tokens_details with reasoning_tokens, queue_time/prompt_time/etc. timing fields on Usage, composes_native_output_with_tools flag, and from_http_response error propagation.
- **RED**: diff upstream changes against current Crystal port
- **GREEN**: port SystemInstructionsPlacement, completion_tokens_details, timing fields
- **GREEN**: wire from_http_response into OpenAI error paths
- **Gates**: existing openai specs pass; new features have spec coverage
- **Target**: 3-5 commits

### R4: Sync Anthropic provider to upstream
**Size**: Medium (~150 lines)
**Depends on**: None
**What**: Citations, system role messages, tool cache control updates. CitationsConfig enum, system_role mid-conversation support.
- **RED**: diff upstream changes (citations, system_role)
- **GREEN**: port Citation enum, CitationsConfig
- **GREEN**: add system_role mid-conversation message support
- **Gates**: existing anthropic specs pass; new citations spec passes
- **Target**: 2-3 commits

### R5: Sync remaining providers (non-Ollama)
**Size**: Small each (~20-50 lines per provider)
**Depends on**: None
**What**: Cohere, Groq, Mistral, xAI, OpenRouter, Gemini, VoyageAI, etc.
- Mistral: streaming refactor (MistralCompatibleProfile, tool_choice serialization)
- Ollama: ✓ (already done)
- Others: model constants, from_http_response wiring, small serialization fixes
- **Gates**: provider-specific specs pass
- **Target**: batch small providers per commit

### R8: AgentRun streaming support (streamed.rs)
**Size**: Large (~300 lines)
**Depends on**: C5, C8
**What**: Port `agent/run/streamed.rs` — StreamedTurnAssembler (sans-IO accumulator), PartialStreamedTurn, StreamedResolution, StreamedTurnEvent. Handles text/reasoning/tool-call deltas, identifies invalid tool calls mid-stream.
- **RED**: write spec for streamed turn assembly from deltas
- **GREEN**: port StreamedTurnAssembler with buffer/replay logic
- **RED**: write spec for streamed_turn → AgentRun integration
- **GREEN**: implement streamed_turn, record_streamed_completion_call
- **Gates**: streaming agent examples work end-to-end
- **Target**: 3-5 commits

### R9: Cleanup old hook types
**Size**: Small (~100 lines removed)
**Depends on**: R1
**What**: Remove PromptHook, HookAction, ToolCallHookAction, InvalidToolCallResolution from prompt_request.cr. Migrate remaining spec helpers and spec tests to use AgentHook directly (no PromptHookAdapter).
- **RED**: identify all references to old hook types in specs
- **GREEN**: migrate each spec to AgentHook, remove old types from source
- **Gates**: all specs pass; old types gone from source and spec helpers
- **Target**: 2-3 commits (spec migration is more involved than source removal)

### R10: Add JSON::Serializable to AgentRun for full state
**Size**: Small (~30 lines)
**Depends on**: C5
**What**: Currently AgentRun serializes only pending calls and new_messages manually. Add full `include JSON::Serializable` with `@[JSON::Field(ignore: true)]` on transient fields.
- **Gates**: serialization spec passes; durable_approval example works
- **Target**: 1 commit

### R11: Update plans/parity.md and inventory TSVs
**Size**: Small (docs only)
**Depends on**: R1-R10
**What**: Update pinned upstream commit, mark completed features, regenerate source/test parity manifests.
- Run: `./scripts/ensure_parity_plan.sh . vendor/rig/crates/rig-core rust auto 0`
- Update parity.md with all completed chunks
- **Target**: 1 commit

---

## Implementation Order (Updated)

```
R1, R2, R6, R7 ──── Completed

Remaining (any order):
  R3 (OpenAI sync)          — large, high value
  R4 (Anthropic sync)       — medium
  R5 (remaining providers)  — small per-provider
  R8 (streamed.rs)          — large, unlocks full streaming
  R9 (old hook cleanup)     — R1-dependent, removes deprecated path
  R10 (JSON serialize)      — small, C5-dependent
  R11 (docs)                — last
```

**Recommended next**: R9 (cleanup old hooks) — smallest remaining, eliminates deprecated PromptHook path entirely. Then R3 (OpenAI, highest parity impact) followed by R4 (Anthropic) and batching remaining R5 providers.
