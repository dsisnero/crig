# v0.40.0 Porting Plan

**Upstream release:** `v0.40.0` (commit `a551c4c5c5df5d26b07111c722cc26ffb2777561`)
**Previous baseline:** `v0.39.0` (commit `06bc651f4c64d1673ba6af698f6c66602c5d313f`)
**Vendor:** `vendor/rig` now at `v0.40.0`

## Summary

148 source files touched in `crates/rig-core/`, +27,012 / −16,605 lines.
Many changes already ported in the crig v0.40.0 release.

---

## Completed

- [x] `129391c` — Remove unused Anthropic decoders (deleted `decoders/` dir, `decoders.cr`, removed require chain)
- [x] `0a328b4` — Paper cuts: ThinkTool moved to `tool/builtin/`, `tools/` dir removed, `UPSTREAM_COMMIT` bumped
- [x] `21be6f1` — Unused derive/core APIs — already clean (no stale `ToolDef`/`ToolSet` aliases)
- [x] `aebf757` — Unused stream completion stdout helper — already absent; high-level `stream_to_stdout` retained (matching upstream)
- [x] `d799996` — Unused generation wrapper traits — already clean
- [x] `8693f13` — Evals module: kept (has active tests; upstream deleted unused Rust code)

## Phase 2 — Core API changes

- [x] `12515a8` — Expand `rig::prelude`: add missing re-exports to `src/crig.cr` / `src/crig/prelude.cr` — all types already available under `Crig::` namespace (Crystal doesn't need Rust-style prelude module)
- [x] `a26409c` — Unify `PromptResponse` and `FinalResponse` into single `PromptResponse` type:
  - Removed `FinalResponse` struct from `streaming.cr`
  - Added `content` and `empty` to `PromptResponse` struct
  - `MultiTurnStreamItem(R)`, `StreamedAssistantContent(R)`, `MultiTurnStreamingResult(R)` now use `Crig::PromptResponse` as generic param
  - `.response` → `.output`, `.history` → `.messages` on `PromptResponse`
  - Updated all source and example references; fixed ~40 spec assertions
- [x] `18f7547` — Enforce exact max turns budget:
  - `max_turns` now bounds exact model calls (0=no calls, 1=initial only, 2=initial+tool continuation)
  - Updated `run.cr`, `runner.cr`, `prompt_request.cr`, `streaming.cr` budgeting logic and defaults
  - Updated upstream-parity tests in `spec/crig/agent/`

## Phase 3 — Provider changes (refactoring to GenericCompletionModel)

Large effort — each provider needs its request/response model updated.

- [x] `2fc78dd` — OpenRouter: migrated to generic completion model
- [x] `49532e0` — Llamafile: already a thin wrapper delegating to `@client.completions_client.completion_model(@model)` (no GenericCompletionModel migration needed)
- [x] `e8cc0ca` — Provider model updates (see breakdown below)
- [x] `695490d` — Remove galadriel provider

### e8cc0ca migration status (OpenAI-compatible providers using `GenericCompletionModel.send_completion_request`)

| Provider | Status | Notes |
|---|---|---|
| OpenRouter | ✓ Done | First migrated |
| Perplexity | ✓ Done | |
| Together | ✓ Done | |
| Groq | ✓ Done | |
| Hyperbolic | ✓ Done | |
| DeepSeek | ✓ Done | |
| HuggingFace | ✓ Done | |
| Mira | ✓ Done | |
| ZAI | ✓ Done | Additional — listed as new in crig |
| XiaomiMimo | ✓ Done | Additional — listed as new in crig |
| MiniMax | ✓ Done | Additional — listed as new in crig |
| Moonshot | ✓ Done | Additional — listed as new in crig |
| xAI (Responses) | ✓ Done | Migrated to GenericCompletionModel |
| Mistral | ✓ Done | Migrated completion to GenericCompletionModel |
| Cohere | ✓ Done | Migrated completion to GenericCompletionModel |
| Copilot | ✓ Done | Replaced provider_error with from_http_response |
| Azure | ✓ Done | Uses `chat_completion_path` + `send_completion_request` |
| Gemini | ✓ Done | Migrated completion to `send_completion_request` |
| Ollama | ✓ Done | Migrated completion to `send_completion_request` |

Shared helpers in `src/crig/providers/internal/generic_completion_model.cr`:
- `send_completion_request` — Creates telemetry span, POSTs JSON, checks status, yields parsed response, records telemetry
- `send_streaming_request` — Same for SSE streaming (requires hash-based `post_json`)

## Phase 4 — Agent runner & fixes

- [x] `d03689a` — Telemetry: keep GenAI message span fields empty:
  - Removed `record_model_input`/`record_model_output` from `SpanCombinator`
  - Stopped populating `gen_ai.input.messages` in `chat_span`
  - Removed corresponding test calls in `spec/crig_spec.cr`
- [x] `4c25682` — Flatten Tool metadata API (verify `tool.cr` `ToolDef` usage matches upstream `Tool` trait):
  - Replaced `ToolDyn#definition(prompt)` with `ToolDyn#description` + `ToolDyn#parameters`
  - Added `Crig.tool_definition(tool)` helper that uses registered name as source of truth
  - Updated `ToolSet#get_tool_definitions` and `ToolSet#documents`
  - Updated `rig_tool` macro, all source implementations (agent, extractor, think, mcp, vector_index)
  - Updated all examples and spec files
  - Renamed `Agent#description`/`Agent#parameters` to `#tool_description`/`#tool_parameters` to avoid conflict with agent's own `getter description`
- [x] `9d74cc5` — ChatGPT: preserve non-success response errors:
  - Replaced `provider_error` with `from_http_response` in both completion and stream paths
  - Non-2xx responses now preserve HTTP status and body via `provider_response_status()` / `provider_response_body()`

## Phase 5 — Examples parity

- [x] `agent_with_approval_policy` — exists and compiles (upstream parity maintained)
- [x] `agent_with_durable_approval` — exists and compiles (repaired from tool-flatten corruption)
- [x] `agent_with_human_in_the_loop` — exists and compiles (upstream parity maintained)
- [x] `force_tool_first_turn` — ported to `examples/force_tool_first_turn/`
- [x] `tool_result_outcomes` — exists and compiles (upstream parity maintained)
- [x] `request_hook` — exists and compiles (upstream parity maintained)

## Phase 6 — Inventory & parity

- [x] Update `plans/inventory/rust_source_parity.tsv` to cover v0.40.0 source files (verified via `ensure_parity_plan.sh`)
- [x] Update `plans/inventory/rust_test_parity.tsv` to cover v0.40.0 test files (verified via `ensure_parity_plan.sh`)
- [x] Run `make format && make lint && make test` (all 1403 pass, format + lint clean)
- [x] Run parity verification scripts (both `ensure_parity_plan.sh` and `verify_parity_adversarial.sh` pass)
